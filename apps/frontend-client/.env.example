# API Configuration
VITE_API_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000

# Note: Google Gemini API key is configured in the backend for security
# The AI chat (LUMO) calls go through the backend API at /api/client/ai/chat
